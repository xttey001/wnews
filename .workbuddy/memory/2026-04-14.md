# 2026-04-14 工作记录

## 早报问题诊断与修复

### 问题诊断结果

#### 问题1：为什么把4.13的新闻放到4.14号
**结论**：这是**预期行为**，不是bug。

**原因**：
- 早报在早上9点运行时，新闻源通常还没有当天的新内容
- `search_today.py` 会创建当天的新闻文件（如 `news_2026-04-14.json`）
- 但内容是昨天的新闻，因为早上9点市场刚开盘

**数据结构**：
- `news_2026-04-13.json`：包含完整的4.13早报+晚报数据
- `news_2026-04-14.json`：包含4.12-4.13的新闻（用于4.14早报）

---

#### 问题2：为什么唐僧没有给出唐憎结论
**结论**：字段缺失bug，已修复。

**原因**：
- `daily_update_v4.py` 生成的 `tang_sanzang` 字段缺少 `唐憎结论` 字段
- 前端 `index.html` 第1245行期望这个字段：`const tsNote = ts.唐憎结论 || ts.conclusion || '';`

**修复内容**：
```python
# daily_update_v4.py 第375-390行
risk_summary = ''
risk_checks = tang_result.get('risk_checks', [])
high_risks = [c for c in risk_checks if '高风险' in c.get('status', '') or '必须' in c.get('status', '')]
if high_risks:
    risk_summary = f'（注意：{len(high_risks)}项高风险）'

tang_conclusion = f'{tang_action_text}，胜率{tang_wr_text}{risk_summary}'

data['tang_sanzang'] = {
    '仓位': tang_position,
    '最终行动': tang_action_text,
    '唐憎结论': tang_conclusion,  # 【新增】
    ...
}
```

**验证结果**：
- `news_2026-04-14.json` 现在包含：`"唐憎结论": "双主线配置：石油ETF+芯片ETF，胜率~50%（注意：1项高风险）"`
- 网站已更新：https://xttey001.github.io/wnews/

---

## 待优化项
1. 唐僧仲裁输出显示 `行动=? | 胜率=?`，需要进一步检查 `tang_result` 的数据提取逻辑

---

## 晚报"待更新"Bug全面修复（21:50）

### 根因（双重Bug叠加）

**Bug #1: `daily_update_v4.py` Step 8 缺少 market_tone 更新**
- 搜索脚本 Step 1 设定 `market_tone.晚报 = "待更新"`
- Steps 5-8 跑完五维分析后，**从未回写** market_tone
- 导致网站始终显示"【2026-04-14晚报】待更新"

**Bug #2: `generate_full_newsdata.py` Step 10 覆盖v4字段**
- 重跑v3管道时强制覆盖 `bajie_conclusion` 为旧版（第120行）
- v4增强字段（wukong_enhanced/tang_sanzang）条件判断有缺陷导致跳过保留
- **结果：自动化显示"成功"，但推送到GitHub的是不完整数据**

### 修复方案

**文件1: daily_update_v4.py (Step 8)**
```python
# 新增：更新 market_tone 晚报内容
data['market_tone'] = {
    '早报': morning_tone,
    '晚报': f'【{today}晚报】{tang_action_text} | 胜率{tang_wr_text} | {tang_position}'
}
```

**文件2: generate_full_newsdata.py (Step 10)**
- `bajie_conclusion`: 检测v4特征字段（融合说明/_experiences_applied），有则保留v4版
- `wukong_enhanced`: 直接从 day_data 取已有值
- `tang_sanzang`: 直接从 day_data 取已有值
- `market_tone`: 从 day_data 取（类型安全检查 isinstance(dict)）

### 验证结果
```
[v4保护] bajie_conclusion 保留v4增强版 ✅
[v4保护] wukong_enhanced 保留           ✅
[v4保护] tang_sanzang 保留              ✅  
[v4保护] market_tone 保留 (双主线配置：石油ETF+芯片ETF | 胜率~70%) ✅
JS语法: OK ✅ | 文件大小: 387KB | 已推送GitHub Pages

---

## 复盘页面(review.html)"正在加载"Bug修复（22:19）

### 根因
- `market_tone` 从字符串改为对象格式 `{早报:"...",晚报:"..."}`
- review.html 的 `calcSummary()` 函数仍用 `tone.match()` 当字符串处理
- 导致 `tone.match is not a function` JS报错，initPage()中断，页面永远显示"正在加载"

### 另一个关键问题
- 之前对话中做了修复（兼容对象格式）但**忘记 git commit + push**
- 导致线上版本一直报错，本地文件正常

### 修复内容
review.html L412-418: 兼容新旧两种market_tone格式
```javascript
const rawTone = day.market_tone || '';
const tone = typeof rawTone === 'string' ? rawTone : (rawTone['晚报'] || rawTone['早报'] || '');
if(typeof tone === 'string' && tone.match(/涨|多|反弹/))sc.偏多++;
// ... 后续同理
```

### 验证
- Playwright 线上测试: No browser errors detected ✅
- 报告内容正常渲染 ✅

---

## 复盘页面3个问题修复（23:53）

### 问题1: 04-14市场基列为空 ✅已修
**根因**: `renderDayRow()` 取 `wj.market_sentiment`，但4.14号只有 `market_view:"谨慎偏空"`，没有 `market_sentiment` 字段
**修复**: `sent = wj.market_sentiment || wj.market_view || ''` — 兜底取 market_view

### 问题2: 实际走势显示"回调"但实际大涨 ✅已修
**根因**: 硬编码只覆盖4.7-4.11，4.14走else默认值"指数回调"
**实际**: 创业板+2.36%，科创综指+2.13%，深证成指+1.61% — 明显大涨！
**修复**: 新增 knownOutcomes 映射表 + outcome-bullish红色高亮样式

### 问题3: 新闻是旧的（非今日A股行情）🚨 根因确认
**搜索结果分析**:
- 19条新闻中 **0条A股行情类**
- 16/19条 = 美股财报(艾伯森/Adobe/特斯拉/CarMax/Bloom/强生) + 地缘(霍尔木兹/沙特)
- 缺失关键: 创业板大涨、AI算力领涨、成交额等

**根本原因**: `search_today_v2.py` 用**新浪财经全球滚动新闻API**(feed.mix.sina.com.cn/api/roll/get?pageid=153&lid=2516)
- 这个API返回的是**全球财经快讯**，以美股、地缘为主
- A股收盘后的行情总结类新闻搜不到
- 需要增加A股专用数据源
```

